

const Home = () => {
    // const authaInfo = useContext(AuthContext)
    // console.log(authaInfo);

    return (
        <div>
            <h2 className="text-2xl">This is Home for: </h2>
        </div>
    );
};

export default Home;